import { KategorieToBezeichnungPipe } from './kategorie-to-bezeichnung.pipe';

describe('KategorieToBezeichnungPipe', () => {
  it('create an instance', () => {
    const pipe = new KategorieToBezeichnungPipe();
    expect(pipe).toBeTruthy();
  });
});
