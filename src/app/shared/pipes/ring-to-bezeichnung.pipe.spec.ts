import { RingToBezeichnungPipe } from './ring-to-bezeichnung.pipe';

describe('RingToBezeichnungPipe', () => {
  it('create an instance', () => {
    const pipe = new RingToBezeichnungPipe();
    expect(pipe).toBeTruthy();
  });
});
